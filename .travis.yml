language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - prp install coveralls
# command to run tests
script: 
  - python manage.py run_test

after-success:
  - coveralls